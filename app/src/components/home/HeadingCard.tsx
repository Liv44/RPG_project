import { FC } from "react";

interface HeadingCardProps {
  name: string;
  rank: number;
  characterID: number;
}

export const HeadingCard: FC<HeadingCardProps> = ({
  name,
  rank,
  characterID,
}) => {
  return <></>;
};
